import LandingLayouts from "@/Components/templates/LandingLayout";

export default function StructurePage() {
    return (
        <div className="">
            StructurePage
        </div>
    );
}

StructurePage.layout = (page) => <LandingLayouts children={page} />;
